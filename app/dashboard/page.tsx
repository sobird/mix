'use client';

import { Button } from 'antd';

const DashBoard = () => {
  return (
    <div>
      DashBoardPage
      <Button onClick={() => {

      }}
      >
        dispatch

      </Button>
    </div>
  );
};

export default DashBoard;
